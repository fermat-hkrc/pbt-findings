---
id: OH-2026-ARKUI-015
date: "2026-09-10"
repo: arkui_ace_engine
repo_url: https://gitcode.com/openharmony/arkui_ace_engine
title: "[Bug]: Quaternion::Slerp at t=0 returns -this when from·to < 0"
cwe: CWE-682
cwe_name: Incorrect Calculation
severity: MEDIUM
status: CONFIRMED_REAL
affected_version: "master"
component: frameworks/base/geometry/quaternion.cpp
file_paths:
  - frameworks/base/geometry/quaternion.cpp
  - frameworks/base/geometry/quaternion.h
author: Toan
internal_issue_id: DTS2026091029544
language: C++
---

## Summary

When `dot(this, q) < 0`, `Slerp` flips **`from`** (`*this`) for the short arc.
At `t=0` the interpolant is then `−this`, not `this`. q and −q are the same
rotation, so matrices still match; sign-sensitive 4-vector consumers see a flip.

**Confirmed** under `DTS2026091029544` (accepted; **not fixed yet**).

## Contract

Header: t∈[0,1] interpolates between this and q. Cited euclideanspace: 0.0 at
from. `from.Slerp(to, 0.0)` equals `from` component-wise.

## Vulnerable Code

```cpp
if (cosHalfAngle < 0.0) {
    from = from.flip();   // flips the t=0 endpoint
}
```

## Minimal Counterexample

`from=(-0.5,-0.5,-0.5,-0.5)`, `to=(0,0,0,1)`, `t=0` → `+from` (expected `from`).

## How PBT Detected This

`TZeroEqualsThis` FAIL; `TZeroSameRotation` PASS.

## Suggested Fix

Flip **`to`**, not `from`: `return Slerp(to.flip(), t);`.
